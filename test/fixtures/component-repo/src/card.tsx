// A component module that knows nothing about the workbench: no story
// file, no import from it, no named exports. Its default export is the
// story, and its own name is the label.

export default function Card() {
  return null;
}
