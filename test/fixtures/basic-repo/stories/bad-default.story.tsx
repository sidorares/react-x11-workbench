// Off-contract on purpose: the default export of a story file is FileMeta,
// not a component. The component export beside it must still be found.

export default () => null;

export const ok = () => null;
