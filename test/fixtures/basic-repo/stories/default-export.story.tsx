// The bottom rung, in a story file: a default export that is a function is
// the file's component, not its FileMeta. Anonymous on purpose — the label
// has to fall back to the filename.

export default () => null;

export const ok = () => null;
